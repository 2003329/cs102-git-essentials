visual studio code(VSC) can be utilized to code python,HTML etc,edit text files,CSV files,markdown files.Visual Studio Code is a manager. We can likewise involve the VSC as a git center site. When we open the git center site in VSC we have a choice called markdown across the board, assuming that we introduce it we can notice the progressions we made. We can make documents with a specific construction and strategy. It assists us with getting sorted out our reports utilising markdown programming. We can likewise see storehouses which we made on GitHub in Visual studio
code.
